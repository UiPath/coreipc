import { expect } from 'chai';
import { ipc } from '../../src/node';

describe('ipc.callback', function () {
    // it('should not throw ', () => {
    //     class SomeCallback {

    //     }

    //     const act = () => ipc
    //         .callback
    //         .forWebSocket('ws://foobar.com')
    //         .andCallbackType(SomeCallback)
    //         .is(new SomeCallback());

    //     expect(act).not.to.throw();
    // });
});
