import {
    CancellationToken,
    TimeSpan,
} from 'foundation';

import { Socket } from 'io';

export abstract class Address {
    public abstract connectAsync(timeout: TimeSpan, ct: CancellationToken): Promise<Socket>;
}
